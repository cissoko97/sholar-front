import { SiderDirective } from './sider.directive';

describe('SiderDirective', () => {
  it('should create an instance', () => {
    const directive = new SiderDirective();
    expect(directive).toBeTruthy();
  });
});
